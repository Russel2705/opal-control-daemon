# paid atau free
MODE=paid

# Telegram
BOT_TOKEN=YOUR_TELEGRAM_BOT_TOKEN
ADMIN_ID=0

# DB
DB_PATH=/var/lib/opal-daemon/app.db

# Server info
ZIVPN_HOST=id.xstrore1.cloud
MAX_ACTIVE=20

# Kartu server (tampilan)
SERVER_TITLE=🟦 🇲🇨ID SERVER
QUOTA_GB=150
IP_LIMIT=2
DISPLAY_PRICE_PER_DAY=333
DISPLAY_PRICE_PER_MONTH=9990

# ===== FREE access control (hanya dipakai saat MODE=free) =====
FREE_ACCESS=public          # public / private
FREE_REQUIRE_CHANNEL=       # opsional: @channelanda (wajib join)

# Harga paket (dipakai MODE=paid)
PRICE_1=1000
PRICE_14=12000
PRICE_30=25000

# Pakasir (dipakai MODE=paid)
PAKASIR_SLUG=your_project_slug
PAKASIR_API_KEY=your_pakasir_api_key

# Webhook (dipakai MODE=paid)
WEBHOOK_PATH=/pakasir/webhook
PORT=9000

# SSL email (dipakai MODE=paid)
CERT_EMAIL=you@mail.com

TZ=Asia/Jakarta
NODE_ENV=production